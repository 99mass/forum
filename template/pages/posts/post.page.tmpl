<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/static/css/header.css">
  <link rel="stylesheet" href="/static/css/post.css">
  <!-- cdn font awsome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"
    integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA=="
    crossorigin="anonymous" referrerpolicy="no-referrer" />
  <title>forum</title>
</head>

<body>

  <!-- begin header -->
  {{template "header" .}}
  <!-- end header -->

  <!-- begin body -->
  <main class="container">

    <h1>
      {{.Error}}
    </h1>
    <div class="container-2">
       <!-- block post and comment -->
       <!-- category list -->
      <h3>
      {{range .Category}}     
         {{.NameCategory}} <span>/</span>
      {{end}}
    </h3>
      <div class="post-displaying">
        <div class="categorie-specifique">
          <!-- posts -->
          <div class="title"><span><i class="fa-solid fa-feather"></i> Title:</span>{{.PostData.Posts.Title}}</div>

          <div><i class="fa-solid fa-quote-left"></i> {{.PostData.Posts.Content}}<i class="fa-solid fa-quote-left"
              style="margin-left: 5px;"></i> </div>

          <div class="author-date">
            <div><span><i class="fa-solid fa-at"></i>author:</span>{{.PostData.User.Username}}</div>
            <span class="bar">|</span>
            <div><span><i class="fa-solid fa-calendar-days"></i>date:</span>{{.PostData.Posts.CreatedAt}}</div>
          </div>

          <div class="like-dislik-content">
            <form action="" method="post">
              <input type="hidden" name="liked" value="">
              <button type="submit"><i class="fa-regular fa-thumbs-up"></i>{{.PostData.PostLike}}</button>
            </form>

            <form action="" method="post">
              <input type="hidden" name="disliked" value="">
              <button type="submit"><i class="fa-regular fa-thumbs-down"></i>{{.PostData.PostDislike}}</button>
            </form>
          </div>
          <!-- Formulaire de comment -->
          <div class="content-form-comment">            
            <form action="/post" method="post">
              <div> <label for="">comment here:</label></div>
              <textarea name="content" id="content" cols="30" rows="6" maxlength="500"
                placeholder="put something here... maximum 500 characters"></textarea>
              {{if .Session}}
              <input type="hidden" name="user_id" id="user_id" value="{{.User.ID}}">
              <input type="hidden" name="post_id" id="post_id" value="{{.PostData.Posts.ID}}">
              <button type="submit">comment</button>
              {{else}}
              <p style="color: red;font-size: 12px;">Please log in to comment a post.</p><br>
              <button type="submit" disabled style="display: flex;"><i class="fa-solid fa-paper-plane" style="color: #221f51;"></i>comment</button>
              {{end}}
            </form>
          </div>
        </div>

        
            <div class="categorie-specifique">
              <!-- comment -->                    
              <h6>all comments</h6>
                <div class="content-comments">
                  {{if len .PostData.Comment}}
                      {{range .PostData.Comment}}
                      <div><i class="fa-regular fa-comments"></i>{{.Comment.Content}}</div>
                      <div class="author-date2">
                        <div><span><i class="fa-solid fa-at"></i>author:</span>{{.User.Username}}</div>
                        <span class="bar">|</span>
                        <div><span><i class="fa-solid fa-calendar-days"></i>date:</span>{{.Comment.CreatedAt}}</div>
                      </div>

                      <div class="like-dislik-content">
                        <form action="" method="post">
                          <input type="hidden" name="liked" value="">
                          <button type="submit"><i class="fa-regular fa-thumbs-up"></i>{{.CommentLike}}</button>
                        </form>
                        <form action="" method="post">
                          <input type="hidden" name="disliked" value="">
                          <button type="submit"><i class="fa-regular fa-thumbs-down"></i>{{.CommentDislike}}</button>
                        </form>
                      </div>
                      <hr>            
                      {{end}}
                  {{else}}
                      <div class="alert1">
                        <strong>Info!</strong> no comment found. be the first to comment this post.
                      </div> 
                  {{end}}
                </div>
            </div>
       
      </div>

      <h4>other post by @{{.PostData.User.Username}}</h4>

      <!--debut  a supprimer- -->
      <div class="post-displaying2">
        {{range .Datas}}
        {{template "post" .}}
        {{end}}
      </div>

  </main>
  <!-- end body -->

  <!--begin footer -->
  {{template "footer"}}
  <!-- end footer -->

  <script src="/static/js/script.js"></script>
</body>

</html>